import { TempLoginPageClient } from "./temp-login-page-client";

export const metadata = {
  title: "Temporary login",
};

export default function TempLoginPage() {
  return <TempLoginPageClient />;
}
